### 26年3月6日
1. 神经网络
将最小的单元作为输入层的神经元，通过神经网络逐步的加权运算在输出层得到最可能的结果
2. 层间运算
神经网络通过对上一层的神经元的激活值进行加权运算
权重告诉当前层的神经元，应该关注什么样的，上一层单元可能的结果
3. 偏置bias
偏置是在加权运算时对结果的一个平移，像是设定了某个阈值，确保加权后超过这个阈值才有效，有意义
4. 机器的学习
每个神经元都和上一层的所有神经元连接，每个连接都有自己的权重，且这一层的每一个神经元都有自己的偏置
机器的学习，就是学习如何设置这些权重和偏置
5. 公式表达
表示这一列激活值组成的列向量与他们权重和，再加上偏置并乘以公式，得到下一列神经元
$$
\sigma \\
\left ( \begin{bmatrix}
w_{0,0} & w_{0,1} & \cdots & w_{0,n} \\
w_{1,0} & w_{1,1} & \cdots & w_{1,n} \\
\vdots & \vdots & \ddots & \vdots \\
w_{k,0} & w_{k,1} & \cdots & w_{k,n}
\end{bmatrix}
\cdot
\begin{bmatrix}
a_0^{(0)} \\
a_1^{(0)} \\
\vdots \\
a_n^{(0)}
\end{bmatrix} +
\begin{bmatrix}
b_0 \\
b_1 \\
\vdots \\
b_n
\end{bmatrix} \right )
=
\begin{bmatrix}
? \\
? \\
\vdots \\
?
\end{bmatrix}
$$
简化为
$$
\mathbf{a}^{(1)} = \sigma\left( \mathbf{W} \mathbf{a}^{(0)} + \mathbf{b} \right)
$$

📌 **公式说明**：

这是神经网络中**单层前向传播**的标准表达式，含义如下：

- $\mathbf{a}^{(1)}$：第 1 层（或下一层）的激活输出向量；
- $\mathbf{a}^{(0)}$：输入层或第 0 层的激活值（输入向量）；
- $\mathbf{W}$：权重矩阵，连接第 0 层与第 1 层；
- $\mathbf{b}$：偏置向量；
- $\sigma(\cdot)$：激活函数（如 Sigmoid、ReLU、Tanh 等），逐元素作用于线性组合结果。
表示从一层到下一层的非线性变换过程。


### 26年3月13日
1. 代价函数cost function
参数是所有的权重和偏置，输出一个值，值越大，代表效果越差
输出的结果是输出层的每个值与预期值的差的平方的和，再取平均。就是代价值，这个函数的输出
针对**整个训练集**。它是所有样本损失值的**平均值**
最终要使得代价函数的值最小，即使是局部最小
2. 梯度下降法
在多元函数中，梯度方向表示该点上升最快的方向，梯度的向量长度表示上升的速度。
这时沿着梯度的反方向，就可以下降，找到局部最小值
梯度向量的每个参数代表了代价函数对于每个参数有多敏感
**计算梯度的算法是神经网络的核心**
3. 反向传播算法back propagation
假设对某一样本进行训练，输出层的结果几乎是随机的。
与预期值相比，相差大的需要调整得更多，相差小的则调整少
根据单层向前传播的公式，要改变激活值，就要调整偏置、权重、上一层的激活值
	1. 权重
		权重的调整和对应的上一层的激活值成正相关。上一层的激活值越大，则对应的权重调整的影响也越大
	2. 激活值
		激活值的调整和对应权重成正相关。
		首先同样的，权重越大，调整激活值的影响越大
		假设我们期望结果是增大，那么负权重的激活值就应该减小，正激活值的权重就应该增大
	3. 偏置
		在公式中，对b求导结果始终是1，因此偏置的调整只取决于后面传回来的误差
	而这一层又与再上一层进行同样的步骤
	我们需要对所有样本的结果进行调整，最后取平均值
5. minibatch
为了权衡速度，将样本分批次，每一批次训练后就更新参数（权重，偏置）。而不是将所有样本都进行反向传播计算调整值，再更新参数。
6. 类似的数学原理
例】设函数y=f(u,v)，其中u=g(x)，v=h(w)，而w=l(x)。求dy/dx的表达式。 这种题老师肯定讲过。一上来老师肯定要你们先画一个有向图，箭尾是自变量，箭头指向因变量，这样一个箭头就表示一个函数。然后下一步老师就要我们从有向图的末尾往“前”反着“倒”，一直“倒”到要求写出的导数的自变量处，然后按走过的路径用链式法则写出偏导数乘积，如果有多条路径就求各路径所得乘积之和。
7. 梯度爆炸、梯度消失
当层数极多，链式法则的连乘会让梯度变得无穷大或者无穷小

### 3月14日
1. LLM(large language model)
大语言模型的输出本质是预测下一个最可能出现的词
大型指的是这个模型有无数的参数，或者说权重，
这些参数是在文本中学习
学习的方法是将一句话都最后一个词去除并进行预测，与预期值比较，并使用反向传播法进行调整参数
完成到这一步称为预训练
之后还需要加入人工干预的训练（强化学习），使得llm的输出结果更符合人类预期
在所有llm中，所有的词都是以数字（向量）编码的，因为神经网络的学习需要连续值
2. transformer的诞生
llm需要的计算量是庞大的，需要GPU的并行计算
但是早期的llm是一个词一个词地进行预测，并不能很好地实现并行
Google提出的名为transformer的新模式
transformer不会从头到尾阅读文字，而是同时并行吸收所有文字信息，
3. attention注意力机制
transformer依赖于一种attention机制，能同时关注到所有的词汇，使得这些词汇（向量）之间能相互“对话”，根据语境完善他们编码的含义
4. 前反馈网络
transformer还有第二种运行方式，即前反馈网络
使得模型能存储更多在训练中学习到更多的语言模式
如果说 Attention 是在处理“词与词的关系”，那么 **FFN 就像是每个词的“私人图书馆”**，它存储了关于这个词的通用知识（比如“巴黎”和“首都”的关联）。
如果想要llm彻底学会词汇的含义与关联，同样需要通过反向传播算法微调ffn中的权重
5. transformer
反复经过attention和前反馈网络这两项基本操作后，llm能在预测最后一个词的时候，更多地结合上下文语境和词汇本身“学到的关于词汇的含义”
6. 向量与词汇
在模型中，含义相同的词，其向量在空间中是非常接近的
7. 幻觉
有时概率最高的一串词（听起来非常通顺、像人话），其内容却是胡编乱造的。这在深度学习中被称为**幻觉（Hallucination）**。
大模型最核心的槽点。它是在模仿人类说话的“规律”，而不是在推理“真理”。