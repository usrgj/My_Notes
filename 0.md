# 第八章 假设检验：第一节、第二节笔记整理

## 第一节 检验的基本原理

### 一、核心概念知识点

1. **假设检验的基本思想**

   假设检验就是利用样本数据判断某个假设是否应该被拒绝。

   它的思路类似“反证法”：

	1. 先假定某个说法成立；
	2. 再看样本数据是否与这个说法严重矛盾；
	3. 如果样本结果在该假设成立时属于小概率事件，就拒绝这个假设。

   简单理解：

   > 先假设它是真的，再看样本数据会不会“打脸”。

2. **原假设 $H_0$**

   原假设又叫零假设，是检验中默认先认为成立的假设。

   常见形式为：

   $$
   H_0:\theta=\theta_0
   $$

   也可以写成：

   $$
   H_0:\theta\le \theta_0
   $$

   或：

   $$
   H_0:\theta\ge \theta_0
   $$

   原假设通常是不想轻易推翻的假设。

3. **备择假设 $H_1$**

   备择假设是与原假设相对的假设，通常表示我们希望通过样本数据支持的结论。

   常见形式有三种：

	1. 双侧检验：

      $$
      H_1:\theta\ne\theta_0
      $$

	2. 右侧检验：

      $$
      H_1:\theta>\theta_0
      $$

	1. 左侧检验：

      $$
      H_1:\theta<\theta_0
      $$



4. **拒绝域 $W$**

   拒绝域是使我们拒绝原假设 $H_0$ 的样本结果范围。

   如果样本结果落入拒绝域：

   $$
   (x_1,x_2,\cdots,x_n)\in W
   $$

   则拒绝 $H_0$。

   如果样本结果没有落入拒绝域，则不拒绝 $H_0$。

5. **接受域**

   接受域就是拒绝域之外的区域。

   如果样本落在接受域内，则不拒绝原假设。

   注意：

   > “不拒绝 $H_0$”不等于“证明 $H_0$ 一定正确”。

   它只表示样本证据不足以推翻 $H_0$。



6. **第一类错误**

    第一类错误是：

    $$
    H_0\text{ 实际成立，但错误地拒绝 }H_0
    $$

    也叫弃真错误。

    它的概率为：

    $$
    P_{\mathrm I}=P(\text{拒绝 }H_0\mid H_0\text{ 成立})
    $$

7. **第二类错误**

    第二类错误是：

    $$
    H_0\text{ 实际不成立，但错误地接受 }H_0
    $$

    也叫采伪错误。

    它的概率为：

    $$
    P_{\mathrm {II}}=P(\text{接受 }H_0\mid H_1\text{ 成立})
    $$

8. **两类错误的关系**

    在样本容量固定时，通常不能同时让两类错误概率都变小。

    一般规律是：

    1. 第一类错误概率变小，第二类错误概率往往变大；
    2. 第一类错误概率变大，第二类错误概率往往变小。

9. **显著性水平 $\alpha$**

    显著性水平 $\alpha$ 是允许犯第一类错误的最大概率。

    即：

    $$
    P(\text{拒绝 }H_0\mid H_0\text{ 成立})\le \alpha
    $$

    常见取值：

    $$
    \alpha=0.05,\quad \alpha=0.01,\quad \alpha=0.10
    $$

10. **显著性水平的直观理解**

    显著性水平就是“最多愿意承担多大概率错杀 $H_0$”的标准。

    $\alpha$ 越小，拒绝 $H_0$ 越谨慎。

    $\alpha$ 越大，越容易拒绝 $H_0$。

11. **检验统计量**

    检验统计量是根据样本构造出来的统计量，用来决定是否拒绝 $H_0$。

    它的关键要求是：

    > 在 $H_0$ 成立时，它的分布必须已知。

    常见检验统计量有：

    $$
    Z,\quad T,\quad \chi^2,\quad F
    $$

12. **$p$ 值**

    $p$ 值是在 $H_0$ 成立的条件下，出现当前样本结果或更极端结果的概率。

    直观理解：

    > $p$ 值越小，说明样本结果越不像是在 $H_0$ 成立时出现的。

13. **$p$ 值检验法**

    判断规则：

    1. 若：

       $$
       p\le \alpha
       $$

       则拒绝 $H_0$。

    2. 若：

       $$
       p>\alpha
       $$

       则不拒绝 $H_0$。

14. **$p$ 值和显著性水平的区别**

    1. $\alpha$ 是检验前人为规定的标准；
    2. $p$ 值是根据样本数据算出来的结果；
    3. $\alpha$ 像“分数线”；
    4. $p$ 值像“实际成绩”；
    5. 比较 $p$ 和 $\alpha$，决定是否拒绝 $H_0$。

---

### 二、第一节做题方法总结

1. **建立假设类题目的方法**

   这类题通常问“如何建立假设检验问题”。

   做题步骤：

   2. 找出题目真正关心的参数或结论；
   3. 把“不想轻易推翻”或“默认成立”的说法放进 $H_0$；
   4. 把题目想证明的方向放进 $H_1$；
   5. 根据 $H_1$ 判断是双侧、右侧还是左侧检验。

   常见表达：

   6. “是否不同”：

      $$
      H_0:\theta=\theta_0,\quad H_1:\theta\ne\theta_0
      $$

   7. “是否提高”：

      $$
      H_0:\theta=\theta_0,\quad H_1:\theta>\theta_0
      $$

      或：

      $$
      H_0:\theta\le\theta_0,\quad H_1:\theta>\theta_0
      $$

   8. “是否降低”：

      $$
      H_0:\theta=\theta_0,\quad H_1:\theta<\theta_0
      $$

      或：

      $$
      H_0:\theta\ge\theta_0,\quad H_1:\theta<\theta_0
      $$

9. **判断检验类型的方法**

   只看备择假设 $H_1$。

   10. 若 $H_1$ 中是“不等于”：

      $$
      \ne
      $$

      则为双侧检验。

   11. 若 $H_1$ 中是“大于”：

      $$
      >
      $$

      则为右侧检验。

   12. 若 $H_1$ 中是“小于”：

      $$
      <
      $$

      则为左侧检验。

13. **判断两类错误的方法**

   这类题通常给出“实际情况”和“检验结论”，让判断犯了哪类错误。

|实际情况|检验结论 |结果|
|---|---|---|
| $H_0$ 成立 | 不拒绝 $H_0$ | 正确 |
| $H_0$ 成立 | 拒绝 $H_0$ | 第一类错误 |
| $H_0$ 不成立 | 不拒绝 $H_0$ | 第二类错误 |
| $H_0$ 不成立 | 拒绝 $H_0$ | 正确 |

   简单记忆：

   14. 拒绝了真的 $H_0$：第一类错误；
   15. 接受了假的 $H_0$：第二类错误。

16. **计算第一类错误概率的方法**

   第一类错误是：

   $$
   H_0\text{ 成立时却落入拒绝域}
   $$

   所以计算公式是：

   $$
   P_{\mathrm I}=P(W\mid H_0\text{ 成立})
   $$

   做题步骤：

   17. 写出拒绝域 $W$；
   18. 在 $H_0$ 成立的条件下确定统计量分布；
   19. 计算落入拒绝域的概率。

20. **计算第二类错误概率的方法**

   第二类错误是：

   $$
   H_1\text{ 成立时却没有落入拒绝域}
   $$

   所以计算公式是：

   $$
   P_{\mathrm {II}}=P(\overline W\mid H_1\text{ 成立})
   $$

   做题步骤：

   21. 写出拒绝域 $W$；
   22. 写出接受域 $\overline W$；
   23. 在 $H_1$ 成立的条件下确定统计量分布；
   24. 计算落入接受域的概率。

25. **确定拒绝域的方法**

   做题步骤：

   26. 根据题目写出 $H_0,H_1$；
   27. 根据 $H_1$ 判断拒绝域方向；
   28. 选择合适的检验统计量；
   29. 利用显著性水平 $\alpha$ 查分位点；
   30. 写出拒绝域。

   常见形式：

   31. 双侧检验：

      $$
      W=\{|z|>u_{1-\frac{\alpha}{2}}\}
      $$

   32. 右侧检验：

      $$
      W=\{z>u_{1-\alpha}\}
      $$

   33. 左侧检验：

      $$
      W=\{z<-u_{1-\alpha}\}
      $$

34. **$p$ 值题目的做法**

   做题步骤：

   35. 写出原假设和备择假设；
   36. 判断是双侧、右侧还是左侧检验；
   37. 计算检验统计量的观测值；
   38. 根据检验方向计算 $p$ 值；
   39. 比较 $p$ 和 $\alpha$；
   40. 得出结论。

   判断规则：

   $$
   p\le\alpha \Rightarrow \text{拒绝 }H_0
   $$

   $$
   p>\alpha \Rightarrow \text{不拒绝 }H_0
   $$

41. **第一节万能做题模板**

   42. 建立假设：

      $$
      H_0:\cdots,\quad H_1:\cdots
      $$

   43. 判断检验类型：

      双侧、右侧或左侧。

   44. 确定拒绝域类型

   45. 若题目要求两类错误概率：

	  1. 第一类错误概率：
	
		 $$
		 P_{\mathrm I}=P(W\mid H_0)
		 $$
	
	  2. 第二类错误概率：
	
		 $$
		 P_{\mathrm {II}}=P(\overline W\mid H_1)
		 $$

   48. 若题目要求 $p$ 值：

      先算统计量观测值，再算“当前值或更极端值”的概率。

   49. 最后写结论：

      1. 落入拒绝域，拒绝 $H_0$；
      2. 不落入拒绝域，不拒绝 $H_0$。

---

## 第二节 正态总体参数的假设检验

### 一、单正态总体均值 $\mu$ 的假设检验

设总体：

$$
X\sim N(\mu,\sigma^2)
$$

样本为：

$$
X_1,X_2,\cdots,X_n
$$

样本均值为：

$$
\bar X
$$

---

### 1. 方差 $\sigma^2$ 已知时：$Z$ 检验

1. **适用条件**

   2. 总体服从正态分布；
   3. 检验总体均值 $\mu$；
   4. 总体方差 $\sigma^2$ 已知。

5. **检验统计量**

   $$
   Z=\frac{\sqrt n(\bar X-\mu_0)}{\sigma}
   $$

   当 $H_0:\mu=\mu_0$ 成立时：

   $$
   Z\sim N(0,1)
   $$

6. **双侧检验**

   假设为：

   $$
   H_0:\mu=\mu_0,\quad H_1:\mu\ne\mu_0
   $$

   拒绝域为：

   $$
   |z|>u_{1-\frac{\alpha}{2}}
   $$

7. **右侧检验**

   假设为：

   $$
   H_0:\mu=\mu_0,\quad H_1:\mu>\mu_0
   $$

   拒绝域为：

   $$
   z>u_{1-\alpha}
   $$

8. **左侧检验**

   假设为：

   $$
   H_0:\mu=\mu_0,\quad H_1:\mu<\mu_0
   $$

   拒绝域为：

   $$
   z<-u_{1-\alpha}
   $$

---

### 2. 方差 $\sigma^2$ 未知时：$t$ 检验

1. **适用条件**

   2. 总体服从正态分布；
   3. 检验总体均值 $\mu$；
   4. 总体方差 $\sigma^2$ 未知。

5. **检验统计量**

   $$
   T=\frac{\sqrt n(\bar X-\mu_0)}{S}
   $$

   当 $H_0:\mu=\mu_0$ 成立时：

   $$
   T\sim t(n-1)
   $$

6. **双侧检验**

   $$
   |t|>t_{1-\frac{\alpha}{2}}(n-1)
   $$

7. **右侧检验**

   $$
   t>t_{1-\alpha}(n-1)
   $$

8. **左侧检验**

   $$
   t<-t_{1-\alpha}(n-1)
   $$

9. **$Z$ 检验和 $t$ 检验的区别**

   10. $\sigma^2$ 已知，用 $Z$ 检验；
   11. $\sigma^2$ 未知，用 $t$ 检验；
   12. $Z$ 检验查标准正态分布表；
   13. $t$ 检验查 $t$ 分布表；
   14. $t$ 检验自由度为 $n-1$。

---

## 二、单正态总体方差 $\sigma^2$ 的假设检验

设总体：

$$
X\sim N(\mu,\sigma^2)
$$

---

### 1. 均值 $\mu$ 未知时

1. **适用条件**

   2. 总体服从正态分布；
   3. 检验总体方差 $\sigma^2$；
   4. 总体均值 $\mu$ 未知。

5. **检验统计量**

   $$
   \chi^2=\frac{(n-1)S^2}{\sigma_0^2}
   $$

   即：

   $$
   \chi^2=\frac{\sum_{i=1}^n(X_i-\bar X)^2}{\sigma_0^2}
   $$

   当 $H_0:\sigma^2=\sigma_0^2$ 成立时：

   $$
   \chi^2\sim \chi^2(n-1)
   $$

6. **双侧检验**

   $$
   H_0:\sigma^2=\sigma_0^2,\quad H_1:\sigma^2\ne\sigma_0^2
   $$

   拒绝域为：

   $$
   \chi^2<\chi^2_{\frac{\alpha}{2}}(n-1)
   $$

   或：

   $$
   \chi^2>\chi^2_{1-\frac{\alpha}{2}}(n-1)
   $$

7. **右侧检验**

   $$
   H_0:\sigma^2=\sigma_0^2,\quad H_1:\sigma^2>\sigma_0^2
   $$

   拒绝域为：

   $$
   \chi^2>\chi^2_{1-\alpha}(n-1)
   $$

8. **左侧检验**

   $$
   H_0:\sigma^2=\sigma_0^2,\quad H_1:\sigma^2<\sigma_0^2
   $$

   拒绝域为：

   $$
   \chi^2<\chi^2_{\alpha}(n-1)
   $$

---

### 2. 均值 $\mu$ 已知时

1. **检验统计量**

   $$
   \chi^2=\frac{\sum_{i=1}^n(X_i-\mu)^2}{\sigma_0^2}
   $$

   当 $H_0:\sigma^2=\sigma_0^2$ 成立时：

   $$
   \chi^2\sim \chi^2(n)
   $$

2. **和 $\mu$ 未知时的区别**

   3. $\mu$ 已知，自由度为 $n$；
   4. $\mu$ 未知，自由度为 $n-1$。

---

## 三、两个正态总体均值差 $\mu_1-\mu_2$ 的假设检验

设：

$$
X\sim N(\mu_1,\sigma_1^2)
$$

$$
Y\sim N(\mu_2,\sigma_2^2)
$$

两个总体相互独立。

样本容量分别为：

$$
m,\quad n
$$

样本均值分别为：

$$
\bar X,\quad \bar Y
$$

---

### 1. $\sigma_1^2,\sigma_2^2$ 已知时：$Z$ 检验

1. **适用条件**

   2. 两个总体都是正态总体；
   3. 两个样本相互独立；
   4. 两个总体方差 $\sigma_1^2,\sigma_2^2$ 已知；
   5. 检验两个总体均值是否有差异。

6. **检验统计量**

   $$
   Z=
   \frac{\bar X-\bar Y}
   {\sqrt{\frac{\sigma_1^2}{m}+\frac{\sigma_2^2}{n}}}
   $$

   当 $H_0:\mu_1=\mu_2$ 成立时：

   $$
   Z\sim N(0,1)
   $$

7. **双侧检验**

   $$
   |z|>u_{1-\frac{\alpha}{2}}
   $$

8. **右侧检验**

   $$
   z>u_{1-\alpha}
   $$

9. **左侧检验**

   $$
   z<-u_{1-\alpha}
   $$

---

### 2. $\sigma_1^2=\sigma_2^2=\sigma^2$ 未知时：两样本 $t$ 检验

1. **适用条件**

   2. 两个总体都是正态总体；
   3. 两个样本相互独立；
   4. 两个总体方差相等；
   5. 共同方差未知。

6. **合并样本方差**

   $$
   S_w^2=
   \frac{(m-1)S_X^2+(n-1)S_Y^2}{m+n-2}
   $$

7. **检验统计量**

   $$
   T=
   \frac{\bar X-\bar Y}
   {S_w\sqrt{\frac1m+\frac1n}}
   $$

   当 $H_0:\mu_1=\mu_2$ 成立时：

   $$
   T\sim t(m+n-2)
   $$

8. **双侧检验**

   $$
   |t|>t_{1-\frac{\alpha}{2}}(m+n-2)
   $$

9. **右侧检验**

   $$
   t>t_{1-\alpha}(m+n-2)
   $$

10. **左侧检验**

   $$
   t<-t_{1-\alpha}(m+n-2)
   $$

11. **若检验 $\mu_1-\mu_2=\delta$**

   若假设为：

   $$
   H_0:\mu_1-\mu_2=\delta
   $$

   则统计量改为：

   $$
   T=
   \frac{(\bar X-\bar Y)-\delta}
   {S_w\sqrt{\frac1m+\frac1n}}
   $$

---

## 四、两个正态总体方差比的假设检验

设：

$$
X\sim N(\mu_1,\sigma_1^2)
$$

$$
Y\sim N(\mu_2,\sigma_2^2)
$$

两个样本相互独立。

---

### 1. $\mu_1,\mu_2$ 未知时：$F$ 检验

1. **适用条件**

   2. 两个总体都是正态总体；
   3. 两个样本相互独立；
   4. 检验两个总体方差是否相等；
   5. 两个总体均值未知。

6. **检验统计量**

   $$
   F=\frac{S_X^2}{S_Y^2}
   $$

   当 $H_0:\sigma_1^2=\sigma_2^2$ 成立时：

   $$
   F\sim F(m-1,n-1)
   $$

7. **双侧检验**

   $$
   F<F_{\frac{\alpha}{2}}(m-1,n-1)
   $$

   或：

   $$
   F>F_{1-\frac{\alpha}{2}}(m-1,n-1)
   $$

8. **右侧检验**

   $$
   F>F_{1-\alpha}(m-1,n-1)
   $$

9. **左侧检验**

   $$
   F<F_{\alpha}(m-1,n-1)
   $$

---

### 2. $\mu_1,\mu_2$ 已知时

1. **检验统计量**

   $$
   F=
   \frac{\frac1m\sum_{i=1}^m(X_i-\mu_1)^2}
   {\frac1n\sum_{i=1}^n(Y_i-\mu_2)^2}
   $$

   当 $H_0:\sigma_1^2=\sigma_2^2$ 成立时：

   $$
   F\sim F(m,n)
   $$

2. **和均值未知时的区别**

   3. 均值已知，自由度为 $m,n$；
   4. 均值未知，自由度为 $m-1,n-1$。

---

## 五、第二节做题方法总结

### 1. 先判断检验对象

1. 检验一个总体均值：

   $$
   \mu
   $$

2. 检验一个总体方差：

   $$
   \sigma^2
   $$

3. 检验两个总体均值差：

   $$
   \mu_1-\mu_2
   $$

4. 检验两个总体方差比：

   $$
   \frac{\sigma_1^2}{\sigma_2^2}
   $$

---

### 2. 再判断已知条件

1. 检验单总体均值时：

   2. 若 $\sigma^2$ 已知，用 $Z$ 检验；
   3. 若 $\sigma^2$ 未知，用 $t$ 检验。

4. 检验单总体方差时：

   5. 若 $\mu$ 已知，用 $\chi^2(n)$；
   6. 若 $\mu$ 未知，用 $\chi^2(n-1)$。

7. 检验两个总体均值差时：

   8. 若 $\sigma_1^2,\sigma_2^2$ 已知，用 $Z$ 检验；
   9. 若 $\sigma_1^2=\sigma_2^2$ 但未知，用两样本 $t$ 检验。

10. 检验两个总体方差比时：

   11. 若 $\mu_1,\mu_2$ 已知，用 $F(m,n)$；
   12. 若 $\mu_1,\mu_2$ 未知，用 $F(m-1,n-1)$。

---

### 3. 最后判断拒绝域方向

1. 双侧检验：

   $$
   H_1:\theta\ne\theta_0
   $$

   拒绝域在两边，一般看绝对值。

2. 右侧检验：

   $$
   H_1:\theta>\theta_0
   $$

   拒绝域在右边，一般是大于某个临界值。

3. 左侧检验：

   $$
   H_1:\theta<\theta_0
   $$

   拒绝域在左边，一般是小于某个临界值。

---

### 4. 第二节万能做题模板

1. 写出假设：

   $$
   H_0:\cdots,\quad H_1:\cdots
   $$

2. 判断检验对象：

   3. 均值；
   4. 方差；
   5. 均值差；
   6. 方差比。

7. 判断已知条件：

   8. 方差是否已知；
   9. 均值是否已知；
   10. 两总体方差是否相等；
   11. 两个样本是否独立。

12. 选择检验统计量：

   13. $Z$；
   14. $t$；
   15. $\chi^2$；
   16. $F$。

17. 写出拒绝域。

18. 代入样本数据，计算统计量观测值。

19. 比较统计量观测值和临界值。

20. 得出结论：

   21. 落入拒绝域，拒绝 $H_0$；
   22. 不落入拒绝域，不拒绝 $H_0$。

23. 用题目语言回答：

   > 在显著性水平 $\alpha=0.05$ 下，可以认为……

   或：

   > 在显著性水平 $\alpha=0.05$ 下，没有充分证据认为……

---

## 六、常见检验方法速查表

| 检验对象 | 条件 | 检验统计量 | 分布 |
|---|---|---|---|
| 单总体均值 $\mu$ | $\sigma^2$ 已知 | $\displaystyle Z=\frac{\sqrt n(\bar X-\mu_0)}{\sigma}$ | $N(0,1)$ |
| 单总体均值 $\mu$ | $\sigma^2$ 未知 | $\displaystyle T=\frac{\sqrt n(\bar X-\mu_0)}{S}$ | $t(n-1)$ |
| 单总体方差 $\sigma^2$ | $\mu$ 未知 | $\displaystyle \chi^2=\frac{(n-1)S^2}{\sigma_0^2}$ | $\chi^2(n-1)$ |
| 单总体方差 $\sigma^2$ | $\mu$ 已知 | $\displaystyle \chi^2=\frac{\sum_{i=1}^n(X_i-\mu)^2}{\sigma_0^2}$ | $\chi^2(n)$ |
| 两总体均值差 $\mu_1-\mu_2$ | $\sigma_1^2,\sigma_2^2$ 已知 | $\displaystyle Z=\frac{\bar X-\bar Y}{\sqrt{\frac{\sigma_1^2}{m}+\frac{\sigma_2^2}{n}}}$ | $N(0,1)$ |
| 两总体均值差 $\mu_1-\mu_2$ | $\sigma_1^2=\sigma_2^2$ 未知 | $\displaystyle T=\frac{\bar X-\bar Y}{S_w\sqrt{\frac1m+\frac1n}}$ | $t(m+n-2)$ |
| 两总体方差比 $\sigma_1^2/\sigma_2^2$ | $\mu_1,\mu_2$ 未知 | $\displaystyle F=\frac{S_X^2}{S_Y^2}$ | $F(m-1,n-1)$ |
| 两总体方差比 $\sigma_1^2/\sigma_2^2$ | $\mu_1,\mu_2$ 已知 | $\displaystyle F=\frac{\frac1m\sum_{i=1}^m(X_i-\mu_1)^2}{\frac1n\sum_{i=1}^n(Y_i-\mu_2)^2}$ | $F(m,n)$ |

---

## 七、最简记忆口诀

1. 检验均值，看方差：

   2. 方差已知用 $Z$；
   3. 方差未知用 $t$。

4. 检验方差，用 $\chi^2$：

   5. 均值已知自由度 $n$；
   6. 均值未知自由度 $n-1$。

7. 两个均值比差异：

   8. 方差已知用 $Z$；
   9. 方差相等未知用两样本 $t$。

10. 两个方差比大小：

   直接用 $F$。

11. 拒绝域方向：

   12. 不等于，看两边；
   13. 大于，看右边；
   14. 小于，看左边。

15. 判断结论：

   16. 落入拒绝域，拒绝 $H_0$；
   17. 没落入拒绝域，不拒绝 $H_0$。

18. $p$ 值判断：

   19. $p\le\alpha$，拒绝 $H_0$；
   20. $p>\alpha$，不拒绝 $H_0$。